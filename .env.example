# Copy to .env (git-ignored). docker compose reads it.
WANDB_API_KEY=
# online (default), offline, or disabled
WANDB_MODE=online
# Your uid/gid, so files the container writes into the checkout are yours:  id -u; id -g
UID=1000
GID=1000
